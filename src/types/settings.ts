export enum AppLogLevel {
    COMBINED = "combined",
    COMMON = "common",
    DEV = "dev",
    SHORT = "short",
    TINY = "tiny",
}

export enum AppMode {
    DEV = "development",
    TEST = "test",
    PROD = "production",
}
